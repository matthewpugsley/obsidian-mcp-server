[project]
name = "obsidian-mcp-server"
version = "0.1.0"
description = "MCP server for reading and searching an Obsidian vault"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.1",
    "python-dotenv>=1.0.0",
    "pyyaml>=6.0.3",
]
